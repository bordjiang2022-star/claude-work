# LiveTranslate v2.0 - Docker Environment Configuration
# Copy this file to .env and fill in your values before running docker-compose

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Alibaba Cloud DashScope API Key (REQUIRED)
# Get your API key from: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=sk-your-api-key-here

# Secret key for JWT token signing (REQUIRED)
# Generate a secure random key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-this-in-production

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Database Configuration
# Default: SQLite in container volume
# For production, consider PostgreSQL: postgresql+asyncpg://user:password@host:5432/dbname
DATABASE_URL=sqlite+aiosqlite:////app/data/livetranslate.db

# Server Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Locale and Timezone
# Supports Chinese (zh_CN), Japanese (ja_JP), and other locales
LANG=C.UTF-8
LC_ALL=C.UTF-8
TZ=Asia/Shanghai

# =============================================================================
# DEVELOPMENT OPTIONS
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable debug mode (not recommended for production)
DEBUG=false

# CORS settings (comma-separated list of allowed origins)
# For development, you might want to add additional origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit the .env file to version control
# 2. Keep your API keys secure and rotate them regularly
# 3. Use strong, unique SECRET_KEY for production deployments
# 4. For Windows testing with different locales (Chinese/Japanese):
#    - Set TZ to appropriate timezone (Asia/Shanghai, Asia/Tokyo, etc.)
#    - Windows Docker Desktop should handle locale display automatically
